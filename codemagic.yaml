workflows:
  ios-build:
    name: iOS Build
    environment:
      flutter: stable

    scripts:
      - flutter pub get
      - flutter build ipa --no-codesign

    artifacts:
      - build/ios/ipa/*.ipa
